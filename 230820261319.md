# MMO Arena refactor — рабочий журнал

Обновлено: 2026-08-23 20:04 +10
Ветка: `refactor/integration-2026-08-21`
Текущий commit: `04b4a1ca`

## Зачем делается рефакторинг

Цель — убрать рассыпанные read/validate/write последовательности из route handlers и заменить их явными domain boundaries с PostgreSQL-транзакциями. Каждая операция должна:

1. проверить provider/user/domain identity;
2. захватить locks в стабильном порядке;
3. вычислить полный результат до SQL mutation;
4. записать все эффекты и terminal status одной транзакцией;
5. отправить notification только после commit;
6. безопасно переживать callback replay и конкуренцию.

Production не является тестовым контуром. Новые slices сначала проходят unit tests, isolated PostgreSQL races, full regression, independent review и controlled dev E2E.

## Архитектурный шаблон

### Payments

```text
provider payment/ledger claim
→ users FOR UPDATE
→ catalog/inventory/bank/premium mutation
→ legacy provider log, если нужен
→ terminal succeeded
→ COMMIT
→ WebSocket notification
```

YooKassa сериализуется строкой `yukassa_payments` (`FOR UPDATE`).
VK сериализуется ledger `payment_deliveries`, UNIQUE `(provider, external_id)`.

### Inventory

Pure recipe functions сначала валидируют полный catalog и существующий inventory, затем строят полный новый inventory без SQL writes. Repository adapters владеют locks и SQL transaction.

## Завершённые slices

### Character ownership

- Убраны stale ownership writes и небезопасные character save paths.
- Ownership проверяется внутри transaction boundary.

### Tutorial

- Убрана двойная награда.
- Skip безнаградный.
- Progress/reward terminal state атомарный.

### Jobs

- Completion settlement подключён ко всем известным путям.
- `start`/`start-random` сериализованы с cancel/settlement.

### Payments — реализованные provider/SKU paths

Для YooKassa и VK реализованы отдельные atomic services:

- silver;
- craft rare/epic;
- rune ×1/×3/×5;
- premium 7/30 дней;
- curse small/large/x50/x100;
- mega/large/craft_rare_200;
- starter pack.

Ключевые commits:

- `c99e2431` — YooKassa silver;
- `1d25388a` — VK ledger/silver;
- `4846a9fd`, `5c66476f`, `fec52fe9` — craft packs;
- `7e90a2ae`, `721c5c12`, `d59fb212` — rune packs/catalog hardening;
- `41d064a3`, `7a0407a9` — premium;
- `b7e8dad7`, `8824ff5e` — curse packs;
- `f5ec4f5f`, `d573046d` — mega/large/rare200;
- `756eff89`, `ac080a2a` — starter;
- `5c813c88` — merge canonical `feature/hunt-map-ui`;
- `6c2a995f` — legacy inventory/catalog compatibility.
- `d378781b` — удалён неатомарный YooKassa fallback; неизвестные SKU fail-closed.
- `f876d8cd` — Yoo/VK small craft переведены на общий inventory validator; общий SQL NULL serializer.
- `7f765223` — recovery script сделан строго read-only; старые generated copies удалены.
- `dd747b79` — из `routes/donate.ts` удалены девять dead mutation helpers и startup DDL; сохранены два authenticated GET endpoint.

### Payment schema

- VK `payment_deliveries` создан и backfilled: 507 unique historical orders.
- Historical identity conflicts: 0.
- YooKassa `payment_id` unique index создан.
- Startup ждёт VK readiness до открытия HTTP и fail-fast завершает процесс при ошибке.

### lastLoginAt

Evidence-only audit. Production: 2622 users, 2622 non-null, 0 null. Backup не содержит полной post-backup login map. Никаких speculative UPDATE не выполнялось.

## Обнаруженные поздние compatibility defects

Эти дефекты показали, почему unit/искусственные fixtures недостаточны.

1. `rarity_id=0` — валидная базовая редкость (`Рунный булыжник`, `Пыль забвения`), но executor запрещал `<=0`.
   - Исправлено на `<0`.
   - Production получил только минимальный compiled hotfix этого условия.
   - Exact production mega catalog dry-run после hotfix: `ok=true`, DB writes 0.

2. Legacy equipment IDs создавались как `Date.now() + Math.random()`.
   - Production: 306 fractional IDs у 77 users.
   - Existing finite positive numeric IDs теперь допустимы.
   - Новые starter IDs остаются UUID-only.

3. Legacy `type:'upgrade'` stacks не имеют equipment `slot`.
   - Теперь валидируются как stack по integer nonnegative `count`.

4. SQL `NULL` inventory нельзя превращать в `[]`.
   - Serializer сохраняет `null`, pure recipe отклоняет non-array fail-closed.
   - Production сейчас: 0 NULL/non-array inventories.

5. Existing stack `count:'2'` раньше принимался через `Number()`.
   - Теперь raw count обязан быть `number`, integer, `>=0`.

## Проверки compatibility commit `6c2a995f`

- focused inventory/starter/mega tests: PASS;
- full server regression: PASS;
- server build: PASS;
- diff check: PASS;
- independent review: PASS;
- isolated real PostgreSQL Yoo race с production-shaped rarity 0: PASS;
- isolated real PostgreSQL VK race с production-shaped rarity 0: PASS;
- temporary test schema удалена.

## Production фактическое состояние

Production уже содержит основной payment runtime и ledger из первого rollout. После реального VK `mega_craft` теста был применён только минимальный compiled hotfix `rarity_id=0`.

Проверено после hotfix:

- PM2 online;
- fresh error log пуст;
- `/api/time` PASS;
- exact production user/catalog mega recipe dry-run `ok=true`;
- DB writes во время dry-run: 0.

VK test order `2352717` остался `pending`, потому что после restart VK не повторил подписанный callback. Вручную reward/status не изменялись.

Остальные изменения commit `6c2a995f` на production не выкладывались.

## Текущая работа

### Dev verification

Dev topology:

- PM2: `game-server-dev`;
- server port: 3002;
- DB: `game_dev`;
- client/nginx: port 3003;
- path: `/opt/game-dev`.

Порядок:

Выполнено:

1. Подтверждены `PGDATABASE=game_dev`, port 3002 и DB identity `game_dev|postgres`.
2. Создан `pg_dump -Fc game_dev`, размер 7,956,151 bytes.
3. Compatibility dist и затем fallback-removal dist загружены в `/opt/game-dev/server/dist/` без `--delete`.
4. Restart выполнялся только для `game-server-dev`.
5. После `d378781b`: PID 456920, PM2 online, `/api/time` PASS, fresh errors отсутствуют.
6. Compiled `routes/yukassa.js`: `processDelivery` отсутствует, marker `Unsupported payment item` присутствует.

Остаётся:

6. Первый controlled E2E выполнен на deployed dev code для `craft_rare_200` у обоих providers.
7. Yoo и VK получили `delivered + already-processed` при двух конкурентных вызовах.
8. У обоих bank увеличился ровно на 2,000,000, inventory counts ровно `[1000,1200]`.
9. Yoo terminal status и VK ledger status стали `succeeded`.
10. Harness удаляет disposable users/orders/ledger rows в awaited `finally`; command завершился exit 0.

Controlled dev E2E расширен на representative effect classes у обоих providers:

- silver 10,000: `delivered + already-processed`, bank +10,000 ровно один раз;
- premium 7d: `delivered + already-processed`, premium продлён ровно на 604,800 секунд;
- `craft_rare`: bank +10,000, stacks `[5,6]` ровно один раз;
- `craft_rare_200`: bank +2,000,000, stacks `[1000,1200]` ровно один раз;
- `ruby_rune_3`: три rune stack по 3, bank без изменений;
- `curse_small`: Soul Crystal ×5, bank +500,000;
- `starter_pack`: 9 equipment с 9 unique UUIDv4, essence ×4, bank +10,000, premium +7d, one-time flag true.

Каждый Yoo/VK test запускал два конкурентных domain/repository вызова и получил `delivered + already-processed`. После E2E read-back: disposable users/Yoo/VK/ledger rows = 0, dev errors отсутствуют, API PASS, PID 458944.

Payment compatibility + controlled dev E2E gate закрыт: итоговый runtime review PASS и dedicated recovery hardening review PASS, BLOCKER/HIGH отсутствуют. Production rollout в этот gate не входит.

### YooKassa recovery hardening

Аудит выявил `src/scripts/processYukassaPayments.ts`, который мог обработать любой pending SKU как premium и раздельно изменить user/payment. Добавлен RED contract; script переписан в read-only audit без SQL mutations. Он только сверяет provider status/identity и рекомендует verified callback retry либо manual review. Пять tracked stale/generated копий из `server/scripts/` удалены; canonical source собирается в `dist/scripts/`. Full regression/build PASS.

### Удаление YooKassa legacy fallback

- Все текущие SKU из `ITEMS` имеют explicit atomic branch.
- Старый `processDelivery` и imports прямых `deliver*` helpers удалены из callback route.
- Unknown/noncanonical item возвращает HTTP 500 и остаётся pending для retry/manual handling; reward и succeeded не записываются.
- Production read-only audit: historical `premium`, `silver_1000`, `silver_5000` rows terminal; pending legacy rows не обнаружены.
- Wiring suite, full regression, build, diff check и independent review: PASS.
- Production не изменён.

### YooKassa startup readiness — текущий slice

Причина: schema DDL выполнялся при import route и завершался silent `.catch(() => {})`, поэтому HTTP мог открыться до готовности таблицы/unique index, а startup скрывал ошибку.

Локальная реализация:

- последовательность сохранена: `CREATE TABLE` → `ALTER item` → `CREATE UNIQUE INDEX`;
- экспортирован `yooKassaPaymentsReady` без silent catch;
- webhook явно ожидает readiness;
- `startServer()` ожидает `Promise.all([vkPaymentsReady, yooKassaPaymentsReady])` до `listen`;
- reject попадает в существующий startup catch и завершает process с кодом 1;
- SKU и delivery branches не менялись.

RED wiring contract подтверждён до реализации. Focused Yoo/VK wiring, full regression, build, diff check и independent review — PASS. Commit `677fd4be` pushed. Dev-only rollout подтверждён: PID 461351, PM2 online, API PASS, fresh errors отсутствуют, compiled readiness export и startup wait присутствуют. Slice завершён; production не изменён.

### Account schema ownership — completed

Read-only live audit подтвердил, что `users.expenabled BOOLEAN DEFAULT true` уже существует и в `game`, и в `game_dev`; owner таблицы `users` — `postgres`. Поэтому route-side `ALTER TABLE ... catch(() => {})` был одновременно лишним и архитектурно неверным: application runtime не должен владеть schema DDL и скрывать permission/schema errors.

Локальная реализация:

- runtime `ALTER TABLE users ... expEnabled` удалён из `routes/account.ts`;
- `expEnabled BOOLEAN DEFAULT TRUE` добавлен в canonical `src/db/schema.sql` для новых установок;
- read/write API `/account/experience-setting` не изменён;
- никаких live DB writes не выполнялось;
- RED→GREEN ownership contract, full regression, build, diff check и independent review — PASS;
- dev-only rollout подтверждён: PID 462041, PM2 online, API PASS, fresh errors отсутствуют, compiled runtime ALTER marker `0`, unauthenticated account endpoint возвращает 401;
- production runtime и schema не изменялись; live production DB использовалась только для read-only проверки существующей колонки.

### Auction users schema ownership — completed

Read-only live audit подтвердил в `game` и `game_dev`: `users.auction_sales INTEGER DEFAULT 0` и `users.overflowmoney INTEGER DEFAULT 0`; owner таблицы `users` — `postgres`. Поэтому route-side user ALTER statements в `routes/auction.ts` имеют тот же ownership defect.

Локальная реализация:

- удалены только два `ALTER TABLE users ...` из `routes/auction.ts`;
- `auction_sales` и `overflowmoney` добавлены в canonical `src/db/schema.sql`;
- auction mutation/repository logic не менялась;
- live DB writes не выполнялись;
- RED→GREEN contract, full regression, build, diff check и independent review — PASS;
- dev-only rollout подтверждён: PID 462611, PM2 online, API PASS, fresh errors отсутствуют, compiled `ALTER TABLE users` marker в auction route `0`, unauthenticated auction endpoint возвращает 401;
- production runtime/schema не изменялись; production DB использовалась только read-only.

### Auction history schema ownership — completed

Read-only audit подтвердил одинаковую `auction_history` в `game` и `game_dev`: owner `game`, exact columns/defaults совпадают с route DDL. Canonical schema таблицу не содержала, а route import выполнял `CREATE TABLE ... catch(() => {})`.

Локальная реализация:

- exact `auction_history` declaration перенесён в `src/db/schema.sql`;
- route-import `CREATE TABLE` и silent catch удалены;
- history queries и auction behavior не изменялись;
- live DB writes не выполнялись;
- RED→GREEN contract, full regression, build, diff check и independent review — PASS;
- commit `cb69e1b9` pushed;
- dev-only rollout подтверждён после intermittent SSH: PID 463322, PM2 online, API PASS, fresh errors отсутствуют, compiled auction route DDL markers `0`, unauthenticated auction endpoint возвращает 401;
- production runtime/schema не изменялись; production DB использовалась только read-only.

### Dice schema ownership — текущий slice

Read-only audit подтвердил одинаковую production-shaped `dice_games` в `game` и `game_dev`; owner таблицы — `postgres`. Canonical `src/db/schema.sql` уже содержит более строгую фактическую схему (`NOT NULL`, defaults, `TIMESTAMPTZ`), а route import содержал более слабый `CREATE TABLE ... catch(() => {})`.

Локальная реализация:

- import-time DDL удалён из `routes/dice.ts`;
- canonical schema не менялась, поскольку уже соответствует live БД;
- status/play/reroll/finish/history logic не менялась;
- live DB writes не выполнялись;
- RED→GREEN contract, full regression, build, diff check и independent review — PASS;
- commit `78006e2a` pushed;
- dev-only rollout подтверждён: PID 464025, PM2 online, API PASS, fresh errors отсутствуют, compiled dice DDL markers `0`, unauthenticated dice status возвращает 401;
- production runtime/schema не изменялись.

## Следующий refactor slice

### YooKassa payments schema/startup ownership — текущий slice

Read-only audit подтвердил identical `yukassa_payments` в `game`/`game_dev`: owner `game`, 197/195 rows, exact live order/types/defaults/nullability, unique named `payment_id` index, duplicate groups `0`, `item IS NULL` rows `0`, необходимые table/sequence privileges. Live DB writes не выполнялись.

Локальная реализация:

- runtime `CREATE TABLE`, `ALTER TABLE ADD item` и unique-index DDL удалены из route;
- canonical declaration и transactional/idempotent migration `yukassaPayments.sql` добавлены;
- live compatibility сохранена: `item TEXT DEFAULT 'premium'` остаётся nullable и последней колонкой, `days` остаётся NOT NULL без default;
- `yooKassaPaymentsReady` теперь read-only exact catalog/index/privilege readiness;
- тот же promise по-прежнему awaited webhook callback и server startup до `listen`;
- provider verification, atomic delivery repositories/services, payment status transitions, notifications/email не менялись;
- migration не запускалась: обе live DB exact-ready;
- ownership contract 4/4, existing payment wiring 6/6, full regression, build, diff check и independent review — PASS; dev verification ожидается;
- production не изменён.

### Bot accounts schema/startup ownership — текущий slice

Read-only audit подтвердил identical live `bot_accounts` в `game`/`game_dev`: owner `game`, 0 rows, exact SERIAL/UNIQUE/default/nullability shape, validated constraints и необходимые runtime privileges. Live DB writes не выполнялись.

Локальная реализация:

- import-time `CREATE TABLE ... catch(() => {})` удалён из `botManager.ts`;
- canonical declaration и transactional/idempotent migration `botAccounts.sql` добавлены;
- runtime выполняет read-only exact catalog readiness: ordered columns/types/nullability/defaults, validated PK/UNIQUE, отдельные table privileges, sequence existence/USAGE;
- `initBotAccounts()` явно awaited до treasury/exchange/WebSocket/listen;
- compiled CommonJS import-order check подтверждает require binding до runtime call;
- bot registration/storage/action behavior не менялось;
- migration не запускалась: обе live DB уже exact-ready;
- RED→GREEN ownership contract, full regression, build, compiled import-order, diff check и independent review — PASS;
- commit `e738b35e` pushed; dev-only rollout подтверждён: PID 475903, PM2/API PASS, fresh errors отсутствуют, readiness/startup markers active, runtime `CREATE bot_accounts` marker `0`;
- production не изменён.

### Treasury schema/startup ownership — текущий slice

Read-only audit подтвердил identical live shape `castle_treasury`/`treasury_log` в `game` и `game_dev`: owner `postgres`, exact defaults/types/nullability, validated singleton CHECK/PK, required grants/sequence USAGE. Singleton row существует ровно одна; `treasury_log` активно используется (60,706 prod / 52,498 dev rows). Live DB writes не выполнялись.

Локальная реализация:

- exact tables добавлены в canonical schema и idempotent migration `src/db/migrations/treasury.sql` с `ON CONFLICT DO NOTHING` seed и minimal grants;
- runtime CREATE/seed writes удалены; readiness read-only проверяет ordered exact columns/defaults, PKs, validated CHECK, all singleton rows и privileges;
- shared `treasuryReady` выполняет initialization ровно один раз;
- HTTP startup ждёт treasury → exchange → WebSocket readiness до `listen`;
- tournament scheduler по-прежнему запускается только если готовы и treasury, и tournament schema;
- treasury mutation/log methods пока не менялись: их atomicity будет отдельным bounded slice;
- ownership contract, full regression, build, diff check и independent reviews — PASS;
- первый dev rollout выявил `ReferenceError: Cannot access 'treasury_1' before initialization`: runtime expression использовал binding импорта, объявленного ниже; все scheduler/game imports перенесены в верхний import block, добавлен import-order contract;
- исправленный artifact повторно доставлен на dev: PID 472707, PM2/API PASS, fresh errors отсутствуют, compiled import order PASS, treasury runtime DDL markers `0`;
- production не изменён.

### Treasury balance/log atomicity — текущий slice

Audit подтвердил, что `addToTreasury`/`deductFromTreasury` выполняли balance UPDATE и audit INSERT двумя autocommit statements. При ошибке второго statement баланс мог измениться без соответствующего лога.

Локальная реализация:

- добавлен единый `changeTreasury(delta, source)` на `db.tx`;
- UPDATE использует signed delta и `RETURNING amount`;
- отсутствие singleton row завершает transaction ошибкой;
- log INSERT выполняется тем же `PoolClient`; ошибка откатывает balance UPDATE;
- публичные add/deduct helpers сохраняют API и делегируют signed delta;
- внешние call sites и атомарность «покупка + налог» намеренно не менялись — это последующие bounded slices;
- RED→GREEN contract и schema ownership contract — PASS; build/diff PASS;
- opt-in `game_dev` integration test использует один `PoolClient` и TEMP shadow tables, поэтому shared dev economy не меняется; он проверяет add/deduct commits, forced log failure rollback и cleanup; реальный PostgreSQL запуск — PASS;
- final independent review — PASS;
- commit `0c79291e` pushed; dev artifact read-back подтвердил transaction markers до restart, после точечного restart `game-server-dev`: PID 475340, PM2/API PASS, fresh errors отсутствуют, `changeTreasuryWithClient`/`RETURNING amount` active, treasury runtime DDL markers `0`;
- production не изменён.

### Exchange schema/startup ownership — текущий slice

Read-only audit подтвердил одинаковые `exchange_gold`/`exchange_history` в `game` и `game_dev`: owner `game`, singleton CHECK/PK, exact defaults, history SERIAL/sequence. В обеих БД существует ровно одна reserve row `id=1`; live DB writes не выполнялись.

Локальная реализация:

- exact tables добавлены в canonical `src/db/schema.sql`;
- idempotent migration `src/db/migrations/exchange.sql` создаёт tables, seed’ит `id=1 amount=28000` только при отсутствии строки (`ON CONFLICT DO NOTHING`) и выдаёт минимальные runtime grants;
- runtime CREATE/seed writes удалены из `initExchange()`;
- runtime readiness read-only проверяет full columns, singleton row, CHECK constraint, table privileges и history sequence USAGE;
- `initExchange()` теперь awaited до WebSocket setup и `server.listen`; из позднего `Promise.allSettled` удалён;
- существующие live reserve values не менялись; AMM formulas, reserve updates, history recording и routes не изменялись;
- первый review нашёл HIGH: readiness проверяла selectability, но не exact types/nullability/defaults/PK; добавлено ordered catalog comparison обеих tables и PK checks;
- re-review нашёл HIGH edge case: singleton query фильтровала `id=1`, а CHECK мог быть `NOT VALID`; теперь читаются все reserve rows, требуется ровно одна строка `id=1`, CHECK должен быть validated;
- после corrections ownership contract, full regression, build и diff check повторно PASS; final re-review PASS без BLOCKER/HIGH; commit `0c68b903` pushed;
- dev-only rollout подтверждён: PID 471014, PM2/API PASS, fresh errors отсутствуют, runtime DDL/seed markers `0`, exact catalog/readiness markers присутствуют, unauthenticated exchange status вернул `401`;
- production не изменён.

### Online activity schema/startup ownership — completed locally/dev

Read-only audit подтвердил production-shaped `game_sessions` и `game_page_visits` в `game`/`game_dev`: owner `game`, BIGSERIAL keys, UNIQUE `(user_id, browser_session_id)`, FK `session_id → game_sessions(id) ON DELETE CASCADE` и четыре аналитических index. Row counts проверены внутри `BEGIN READ ONLY`; live DB writes не выполнялись.

Audit также выявил startup gap: `setupWebSocket(server)` возвращал Promise, но вызывался без `await`, поэтому failure `initOnlineActivity()` не блокировал `server.listen`.

Локальная реализация:

- exact tables/FK/UNIQUE/indexes добавлены в canonical `src/db/schema.sql`;
- добавлена idempotent migration `src/db/migrations/onlineActivity.sql` с explicit table/sequence grants;
- runtime DDL заменён двумя read-only readiness probes;
- readiness выполняется до создания `WebSocketServer`;
- `setupWebSocket(server)` теперь awaited внутри `startServer()` до `server.listen`;
- heartbeat/session/page write logic не менялась, CRLF `websocket.ts` сохранён;
- migration не запускалась: обе live БД уже exact-ready;
- первый review нашёл HIGH: readiness probes проверяли не все constraints/indexes/privileges; добавлена fail-closed catalog validation полного набора runtime columns, exact UNIQUE/FK, каждого index definition и sequence/table privileges;
- re-review нашёл второй HIGH в PostgreSQL semantics: multi-privilege `has_table_privilege(..., 'INSERT, UPDATE')` означает any, а не all; проверки разделены на отдельные INSERT/UPDATE для обеих таблиц;
- после corrections ownership/activity contracts, full regression, build и diff check повторно PASS; final re-review PASS без BLOCKER/HIGH;
- commit `04b4a1ca` pushed;
- dev-only rollout подтверждён: PID 470037, PM2 online, API PASS, fresh errors отсутствуют, runtime DDL markers `0`, full readiness marker `1`, четыре privilege checks и awaited WebSocket startup присутствуют в compiled artifact;
- production не изменён.

### Guild boss weekly state ownership — completed locally/dev

Read-only audit подтвердил одинаковую singleton `guild_boss_weekly_state` в `game`/`game_dev`: owner `game`, `id INTEGER PRIMARY KEY CHECK (id = 1)`, `week_start INTEGER NOT NULL`, runtime privileges присутствуют.

Локальная реализация:

- exact declaration добавлена в canonical `src/db/schema.sql`;
- добавлена idempotent migration `src/db/migrations/guildBossWeeklyState.sql` с explicit grants;
- scheduler-side `CREATE TABLE` удалён;
- startup сохраняет fail-fast readiness через read-only `SELECT week_start ... LIMIT 0` до scheduler loop;
- advisory lock, `FOR UPDATE`, rating awards и reset behavior не менялись;
- migration не запускалась: обе live БД уже exact-ready, live DB writes не выполнялись;
- RED→GREEN contract, full regression, build, diff check и independent review — PASS;
- commits `26b69b41` и `c4c64163` pushed;
- dev-only rollout подтверждён: PID 468009, PM2 online, API PASS, fresh errors отсутствуют, compiled CREATE marker `0`, read-only readiness marker `1`;
- production не изменён.

### Dungeon skill schema ownership — completed locally/dev

Read-only audit подтвердил одинаковые `skill_pages` и `skill_levels` в `game`/`game_dev`: owner `game`, duplicates по `(userId, skillId)` отсутствуют, UNIQUE constraints существуют. Эти constraints критичны для payout `ON CONFLICT` и skill upgrade.

Локальная реализация:

- exact `skill_pages` и `skill_levels` declarations перенесены в canonical `src/db/schema.sql`;
- только их import-time DDL удалён из `routes/dungeon.ts`;
- в этом bounded slice `dungeon_runs` DDL, combat engine, payout repository и upgrade logic не менялись;
- contract закрепляет defaults, UNIQUE keys и реальные read/upgrade/payout paths;
- live DB writes не выполнялись;
- RED→GREEN contract, full regression, build, diff check и independent review — PASS;
- commit `c6082612` создан локально; GitHub push ожидает восстановления connectivity;
- dev-only rollout подтверждён: PID 465855, PM2 online, API PASS, fresh errors отсутствуют, compiled skill table DDL markers `0`; `dungeon_runs` markers остаются `3` по отдельному pending slice; unauthenticated pages endpoint возвращает 401;
- production не изменён.

### Dungeon run schema ownership — completed locally/dev

Read-only audit подтвердил одинаковую `dungeon_runs` в `game`/`game_dev`: owner `game`, duplicates по `userId` отсутствуют, UNIQUE `(userId)` существует, `maxfloor`/`maxreward` уже присутствуют с `DEFAULT 0`.

Локальная реализация:

- exact progress/checkpoint declaration перенесена в canonical `src/db/schema.sql`;
- `CREATE TABLE dungeon_runs` и оба redundant silent `ALTER` удалены из `routes/dungeon.ts`;
- checkpoint, start/upsert, atomic payout progress и combat engine не менялись;
- live DB writes не выполнялись;
- run+skill ownership contracts, full regression, build, diff check и independent review — PASS;
- commit `42c355c4` и последующие commits pushed;
- dev-only rollout подтверждён: PID 466864, PM2 online, API PASS, fresh errors отсутствуют, compiled dungeon route DDL markers `0`, unauthenticated dungeon status возвращает 401;
- production не изменён.

### Dungeon logs schema ownership — completed locally/dev

Read-only audit подтвердил одинаковую `dungeon_logs` в `game`/`game_dev`: owner `postgres`; runtime role `game` имеет table privileges и sequence USAGE. Route таблицу не создаёт и использует её только для optional balance logging.

Локальная реализация:

- exact JSONB-capable declaration добавлена в canonical `src/db/schema.sql`;
- добавлена idempotent migration `src/db/migrations/dungeonLogs.sql` с explicit table grants и sequence privileges;
- route logging behavior не менялся;
- migration не запускалась: обе live БД уже имеют exact table/grants, live DB writes не выполнялись;
- logs/run/skill contracts, full regression, build, diff check и independent review — PASS;
- commit `7cee342b` pushed;
- migration не запускалась: обе live БД уже имели exact table/grants; dev rollout compiled artifact подтверждён вместе с dungeon slice, PID 466864, errors отсутствуют;
- production не изменён.

### Guild buildings schema ownership — текущий slice

Read-only audit подтвердил одинаковую `guild_buildings` в `game` и `game_dev`: owner `game`, `guildId`/`buildingType` NOT NULL, UNIQUE `(guildId, buildingType)`, `level DEFAULT 0`. Canonical schema таблицу не содержала, route import выполнял `CREATE TABLE ... catch(() => {})`.

Локальная реализация:

- exact table declaration вместе с UNIQUE constraint перенесён в `src/db/schema.sql`;
- import-time DDL удалён из `routes/guildBuildings.ts`;
- read/upgrade/insert/update behavior не менялся;
- live DB writes не выполнялись;
- RED→GREEN contract, full regression, build, diff check и independent review — PASS;
- commit `cca7adf7` pushed;
- поздний independent review выявил missing test assertion для `level DEFAULT 0`; contract усилен commit `48d9354a`, focused/full/build/diff PASS; GitHub push этого test-only commit ожидает восстановления connectivity;
- dev-only rollout подтверждён: PID 465129, PM2 online, API PASS, fresh errors отсутствуют, compiled guild buildings DDL markers `0`, unauthenticated buildings endpoint возвращает 401;
- production не изменён.

Read-only audit подтвердил два отдельных hygiene debt:

- `server/src/routes/donate.ts` cleanup завершён в `dd747b79`: удалены девять legacy mutation helpers и startup DDL; сохранены authenticated status/preview GET endpoints, review/dev verification PASS.
- root mirror `server/routes/*`, `server/index.ts`, `server/setupRoutes.ts` не входит в current `tsconfig` (`include: ["src"]`) и npm dev/build/start, но остаётся tracked stale tree. Read-only classification: 466 mirror files; 49 exact duplicates, 51 divergent counterparts, 366 root-only; 274 are generated JS/maps/declarations. Поэтому удаление всего mirror вслепую запрещено: root-only bots/database/scripts требуют отдельного inventory и runtime evidence. Не смешивать с payment slice.

Критерии выбора:

- ограниченный scope;
- измеримый invariant;
- production-shaped fixtures;
- isolated transaction tests;
- без смешивания с UI redesign.

Кандидаты нужно определить read-only audit’ом текущего tree, затем взять один, а не продолжать широкий бесконечный rewrite.

## Правила продолжения

- После каждого завершённого slice обновлять эту заметку.
- Не писать «готово» только по unit tests.
- Реальные provider callbacks/legacy shapes должны иметь fixtures.
- Production deploy только после dev E2E и отдельного решения о rollout.
- Не использовать production для экспериментов.
- Не подделывать VK signatures и не выдавать pending rewards вручную.
- Git staging — exact files, не `git add .`.
- `.env`, `node_modules`, uploads не затрагивать; server rsync без `--delete`.
- Перед production restart — in-memory dungeon deploy guard + warning window.

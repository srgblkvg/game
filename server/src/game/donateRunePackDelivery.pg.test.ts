/// <reference types="node" />
import assert from 'node:assert/strict';import test from 'node:test';import {pool} from '../db/index';import {processYooKassaRunePackPayment} from './donateRunePackDelivery';import {createPgDonateCraftPackRepository} from './donateCraftPackDeliveryRepository';const run=process.env.RUN_PG_TESTS==='1';const pgTest=run?test:test.skip;
pgTest('parallel Yoo rune callbacks deliver three stacks once',async()=>{const uid=Number((await pool.query("INSERT INTO users(username,passwordhash,inventory,bank) VALUES('rune-pg','x','[]',0) RETURNING id")).rows[0].id);const pid='rune-pg-'+Date.now();try{await pool.query("INSERT INTO rarities(id,display_name,color) VALUES(4,'Эпический','#a0f'),(5,'Легендарный','#fa0'),(6,'Мифический','#f00')");await pool.query("INSERT INTO craft_items(id,name,rarity_id,type,image) VALUES(21,'Руна Рубина',6,'upgrade','/ruby'),(22,'Руна Топаза',5,'upgrade','/topaz'),(23,'Руна Аметиста',4,'upgrade','/amethyst')");await pool.query("INSERT INTO yukassa_payments(payment_id,user_id,item,amount,status,processed_at) VALUES($1,$2,'ruby_rune_3','999.00','pending',1)",[pid,uid]);const input={paymentId:pid,providerUserId:String(uid),providerItem:'ruby_rune_3',verifiedAmount:'999.00',verifiedCurrency:'RUB',processedAt:2};const r=await Promise.all([processYooKassaRunePackPayment(createPgDonateCraftPackRepository(),input),processYooKassaRunePackPayment(createPgDonateCraftPackRepository(),input)]);assert.deepEqual(r.map(x=>x.status).sort(),['already-processed','delivered']);const inv=(await pool.query('SELECT inventory FROM users WHERE id=$1',[uid])).rows[0].inventory;const parsed=typeof inv==='string'?JSON.parse(inv):inv;assert.deepEqual(parsed.map((i:any)=>i.count),[3,3,3]);}finally{await pool.query('DELETE FROM yukassa_payments WHERE payment_id=$1',[pid]);await pool.query('DELETE FROM users WHERE id=$1',[uid]);await pool.query('DELETE FROM craft_items');await pool.query('DELETE FROM rarities');}});test.after(async()=>{if(run)await pool.end();});

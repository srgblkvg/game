import type {VkCraftPackRepository} from './vkCraftPackPayment';import {applyInventoryRecipe} from './donateInventoryRecipe';
interface Input{orderId:string;vkUserId:number;item:string;providerPrice:number;processedAt:number;}
type Result={status:'delivered';characterId:number;item:string}|{status:'already-processed'}|{status:'rejected';reason:string};
const SKUS:Record<string,{price:number;multiplier:number}>={ruby_rune_1:{price:57,multiplier:1},ruby_rune_3:{price:144,multiplier:3},ruby_rune_5:{price:214,multiplier:5}};
const RECIPE={entries:[{name:'Руна Рубина',count:1},{name:'Руна Топаза',count:1},{name:'Руна Аметиста',count:1}],bankDelta:0};
export function processVkRunePackPayment(repository:VkCraftPackRepository,input:Input):Promise<Result>{const sku=SKUS[input.item];if(!input.orderId||!Number.isInteger(input.vkUserId)||input.vkUserId<=0||!sku||input.providerPrice!==sku.price)return Promise.resolve({status:'rejected',reason:'payment-mismatch'});return repository.transaction(async tx=>{const claim=await tx.claim({provider:'vk',externalId:input.orderId,providerUserId:input.vkUserId,item:input.item});if(claim.provider!=='vk'||claim.externalId!==input.orderId||claim.providerUserId!==input.vkUserId||claim.item!==input.item)return{status:'rejected',reason:'claim-identity-mismatch'};if(claim.status==='succeeded')return{status:'already-processed'};if(claim.status!=='pending')return{status:'rejected',reason:'claim-status'};const user=await tx.lockVkUser(input.vkUserId);if(!user)return{status:'rejected',reason:'character-not-found'};const catalog=await tx.findCraftItems(RECIPE.entries.map(e=>e.name));const applied=applyInventoryRecipe(user.inventory,catalog,RECIPE,sku.multiplier);if(!applied.ok)return{status:'rejected',reason:applied.reason};await tx.saveUser(user.id,applied.inventoryJson,applied.bankDelta);await tx.logPayment({orderId:input.orderId,vkUserId:input.vkUserId,characterId:user.id,item:input.item,processedAt:input.processedAt});await tx.markSucceeded(input.orderId,input.processedAt,user.id);return{status:'delivered',characterId:user.id,item:input.item};});}

import type {DonateCraftPackRepository} from './donateCraftPackDelivery';import {applyInventoryRecipe} from './donateInventoryRecipe';
const NAMES=['Руна Рубина','Руна Топаза','Руна Аметиста','Руна Сапфира','Руна Изумруда','Рунный булыжник','Рунный белокамень','Пыль забвения','Осколок скорби','Фрагмент ужаса','Эссенция мрака','Сердцевина бездны','Искра погибели','Слеза вечности'];
const RECIPES={mega_craft:{amount:'79999.00',bank:20000000,entries:NAMES.map(name=>({name,count:200}))},large_craft:{amount:'52999.00',bank:10000000,entries:NAMES.map(name=>({name,count:100}))},craft_rare_200:{amount:'19999.00',bank:2000000,entries:[{name:'Сердцевина бездны',count:1000},{name:'Рунный булыжник',count:1200}]}} as const;
export function getMegaPackRecipe(item:string){return RECIPES[item as keyof typeof RECIPES]}
interface Input{paymentId:string;providerUserId:string;providerItem:string;verifiedAmount:string;verifiedCurrency:string;processedAt:number}
type Result={status:'delivered';userId:number;item:string}|{status:'already-processed'}|{status:'rejected';reason:string};const money=(v:string)=>{const n=Number(v);return Number.isFinite(n)?n.toFixed(2):null};
export function processYooKassaMegaPackPayment(repository:DonateCraftPackRepository,input:Input):Promise<Result>{return repository.transaction(async tx=>{const payment=await tx.lockPayment(input.paymentId);if(!payment)return{status:'rejected',reason:'payment-not-found'};if(payment.status!=='pending')return{status:'already-processed'};const recipe=getMegaPackRecipe(payment.item);if(!recipe||payment.item!==input.providerItem||String(payment.userId)!==input.providerUserId||money(payment.amount)!==recipe.amount||money(input.verifiedAmount)!==recipe.amount||input.verifiedCurrency!=='RUB')return{status:'rejected',reason:'payment-mismatch'};const user=await tx.lockUser(payment.userId);if(!user)return{status:'rejected',reason:'user-not-found'};const catalog=await tx.findCraftItems(recipe.entries.map(e=>e.name));const applied=applyInventoryRecipe(user.inventory,catalog,{entries:recipe.entries,bankDelta:recipe.bank});if(!applied.ok)return{status:'rejected',reason:applied.reason};await tx.saveUser(user.id,applied.inventoryJson,applied.bankDelta);await tx.markSucceeded(payment.paymentId,input.processedAt);return{status:'delivered',userId:user.id,item:payment.item};});}

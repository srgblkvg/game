import type {InventoryCatalogItem} from './donateInventoryRecipe';import {applyStarterPackRecipe,type StarterEquipmentTemplate} from './starterPackRecipe';
interface Claim{provider:'vk';externalId:string;providerUserId:number;item:string;status:string}
export interface VkStarterPackTransaction{claim(input:{provider:'vk';externalId:string;providerUserId:number;item:string}):Promise<Claim>;lockUser(vkUserId:number):Promise<{id:number;inventory:string;bank:number|null;premiumUntil:number|null;starterPackPurchased:boolean}|null>;findEquipmentTemplates():Promise<StarterEquipmentTemplate[]>;findEssence():Promise<InventoryCatalogItem[]>;saveStarter(userId:number,inventory:string,bankDelta:number,premiumUntil:number):Promise<void>;logPayment(input:{orderId:string;vkUserId:number;characterId:number;item:string;processedAt:number}):Promise<void>;markSucceeded(orderId:string,processedAt:number,characterId:number):Promise<void>}
export interface VkStarterPackRepository{transaction<T>(cb:(tx:VkStarterPackTransaction)=>Promise<T>):Promise<T>}
interface Input{orderId:string;vkUserId:number;item:string;providerPrice:number;processedAt:number}type Result={status:'delivered';characterId:number;premiumUntil:number}|{status:'already-processed'}|{status:'rejected';reason:string};
export function processVkStarterPackPayment(repository:VkStarterPackRepository,input:Input,createId:()=>string|number):Promise<Result>{if(!input.orderId||!Number.isInteger(input.vkUserId)||input.vkUserId<=0||input.item!=='starter_pack'||input.providerPrice!==14)return Promise.resolve({status:'rejected',reason:'payment-mismatch'});return repository.transaction(async tx=>{const claim=await tx.claim({provider:'vk',externalId:input.orderId,providerUserId:input.vkUserId,item:input.item});if(claim.provider!=='vk'||claim.externalId!==input.orderId||claim.providerUserId!==input.vkUserId||claim.item!==input.item)return{status:'rejected',reason:'claim-identity-mismatch'};if(claim.status==='succeeded')return{status:'already-processed'};if(claim.status!=='pending')return{status:'rejected',reason:'claim-status'};const user=await tx.lockUser(input.vkUserId);if(!user)return{status:'rejected',reason:'character-not-found'};if(user.starterPackPurchased)return{status:'rejected',reason:'starter-already-purchased'};const [templates,essence]=await Promise.all([tx.findEquipmentTemplates(),tx.findEssence()]);const applied=applyStarterPackRecipe(user.inventory,templates,essence,createId);if(!applied.ok)return{status:'rejected',reason:applied.reason};const premiumUntil=Math.max(user.premiumUntil||0,input.processedAt)+7*86400;await tx.saveStarter(user.id,applied.inventoryJson,10000,premiumUntil);await tx.logPayment({orderId:input.orderId,vkUserId:input.vkUserId,characterId:user.id,item:input.item,processedAt:input.processedAt});await tx.markSucceeded(input.orderId,input.processedAt,user.id);return{status:'delivered',characterId:user.id,premiumUntil};});}
